/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MobileAddstoreComponent } from './mobile-addstore.component';

describe('Component: MobileAddstore', () => {
  it('should create an instance', () => {
    let component = new MobileAddstoreComponent();
    expect(component).toBeTruthy();
  });
});
