import { EventEmitter } from "@angular/core";
export declare class ResourceService {
    data: Array<any>;
    keys: Array<any>;
    key: string;
    order: Array<any>;
    previousData: Array<any>;
    private static _pipedDataEmitter;
    static getPipedData(): EventEmitter<any>;
    getOrder(): Array<any>;
    sortBy(key: string): Array<any>;
    private compare(a, b);
}
