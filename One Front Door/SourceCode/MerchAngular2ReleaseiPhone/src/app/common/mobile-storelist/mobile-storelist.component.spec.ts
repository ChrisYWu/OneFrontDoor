/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MobileStorelistComponent } from './mobile-storelist.component';

describe('Component: MobileStorelist', () => {
  it('should create an instance', () => {
    let component = new MobileStorelistComponent();
    expect(component).toBeTruthy();
  });
});
